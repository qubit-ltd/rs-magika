[package]
name = "qubit-magika-dynamic-loader-consumer"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
qubit-magika = { path = "../../..", default-features = false }
ort = { version = "=2.0.0-rc.12", default-features = false, features = ["std", "ndarray", "load-dynamic", "api-24"] }
